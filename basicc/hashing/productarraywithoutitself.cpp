#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int>arr = { 1,2,3,4,-1,-5};
    int n = arr.size();
    vector<int>ans;
    for(int i=0;i<n;i++)
    {
        int product =1;
        for(int j=0;j<n;j++)
        {
            if(nums[i] == nums[j])
            continue;
        }
    }

}